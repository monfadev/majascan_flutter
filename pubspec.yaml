name: majascan
description: A qr code scanner flutter plugin project. Using method channel open native camera page scan QR code. Support iOS, Android.
version: 0.6.0

homepage: https://maja0270558.github.io/majascan_flutter/.

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.djgeo.majascan
        pluginClass: MajascanPlugin
      ios:
        pluginClass: MajascanPlugin

  assets:
    - assets/
